import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xffE50812);
  static const secondary = Color(0xff272b30);
  static const primaryBackground = Color(0xff1a1d1f);
  static const secondaryBackground = Color(0xff272b30);
  static const primaryText = Color(0xffa9aaac);
  static const secondaryText = Colors.white;
  static const primaryBtnText = Colors.white;
  static const error = Colors.red;
  static const black = Colors.black;
  static const inactive = Color(0x26ffffff);
  static const transparent = Colors.transparent;
  static const ratingIcon = Color(0xffffbe21);
  static const circleDot = Color(0x33ffffff);
  static const iconContainer = Color(0xB2272830);

  static const primaryBackgroundLight = Color(0xfff4f5f7); // Lighter background
  static const secondaryBackgroundLight =
      Color.fromARGB(255, 206, 206, 206); // White background for light theme
  static const primaryTextLight =
      Color(0xff333333); // Darker text for readability
  static const secondaryTextLight =
      Color(0xff1a1a1a); // Slightly lighter for secondary text
  static const primaryBtnTextLight =
      Colors.black; // Dark text for buttons in light mode
  static const iconContainerLight =
      Color(0xB2272830); // A slightly lighter container
}
